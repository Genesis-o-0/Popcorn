const NotFound = () => {
  return (
    <div
      style={{
        height: "80vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1 className="text-danger text-center pt-5 px-2">Error 404 page not found</h1>
    </div>
  );
};

export default NotFound;
